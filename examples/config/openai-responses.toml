[model]
name = "default"
provider = "open-ai"
model = "gpt-5.5"
base_url = "https://api.phrolova.uno"
api_key_env = "OPENAI_API_KEY"
openai_protocol = "responses"

[agent]
# 0 表示交互主会话不设置总 ReAct 步数上限。
max_steps = 0
stream = true
