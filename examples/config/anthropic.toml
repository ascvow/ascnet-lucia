[model]
name = "default"
provider = "anthropic"
model = "replace-with-anthropic-model"
api_key_env = "ANTHROPIC_API_KEY"

[agent]
# 0 表示交互主会话不设置总 ReAct 步数上限。
max_steps = 0
max_tokens = 4096
stream = true
