[package]
name = "agent-runtime-plugin"
version = "0.1.0"
edition = "2021"
license = "MIT"
description = "展示 Lucia Agent Runtime 宿主能力的 WASM 测试插件。"

[lib]
crate-type = ["cdylib"]

[dependencies]
anyhow = "1"
agent-plugin = { path = "../../../crates/agent-plugin" }
serde_json = "1"
wit-bindgen = "0.59"
