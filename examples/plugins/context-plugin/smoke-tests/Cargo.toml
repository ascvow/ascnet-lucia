[package]
name = "context-plugin-smoke-tests"
version = "0.1.0"
edition = "2021"
authors = ["ascvow"]
license = "MIT"
description = "官方上下文管理插件的独立端到端测试。"

[workspace]

[dev-dependencies]
anyhow = "1"
async-trait = "0.1"
agent-core = { path = "../../../../crates/ascnet-lucia-core", default-features = false }
agent-plugin-host = { path = "../../../../crates/ascnet-lucia-plugin-host" }
tokio = { version = "1", features = ["macros", "rt-multi-thread"] }
