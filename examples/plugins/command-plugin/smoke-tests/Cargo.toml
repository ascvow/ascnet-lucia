[package]
name = "command-plugin-smoke-tests"
version = "0.1.0"
edition = "2021"
authors = ["ascvow"]
license = "MIT"
description = "官方 Command 插件的独立 WASM 端到端测试。"

[workspace]

[dev-dependencies]
agent-plugin-host = { path = "../../../../crates/agent-plugin-host" }
command-protocol = { path = "../crates/command-protocol" }
serde_json = "1"
tokio = { version = "1", features = ["macros", "rt-multi-thread"] }
